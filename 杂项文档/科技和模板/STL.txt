struct P{
	string name;
	int score;
	bool operator < (cosnt P& rhs) const {
		if (score!= rhs.score) retur score > rhs . score;
		return name < rhs .name ; 
	}
};

struct cmp{
	bool operator () (const P& a, const P& b) const {
		if (x.score != y.score) return x.score> y.score;
		return x.name < y.name ;
	}
};
priority_queue <P,vector<P>, cmp > pq;

multimap<int,int>   multiset<int>

lower_bound  // >=  element
upper_bound  // >   element

resize()

stringsream ss;
while (ss >> s)

char a[10];
int x;
sprintf(a,"%d",x);
const char *s="12798";
int num_s=atoi(s);
const char *ss="123456789";
ll num=atoll(ss);

*max_element     *min_element


C++ 11:
		
auto :
	map<int,int> mp;
	auto it = mp.begin();
	for (;it!=mp,end();++it){
		cout <<it->first << it->second << endl; 
	}
	
	
range for :

set<int> s;

for (auto &i:s) cout << i << endl;


unordered_map


bitwise function :
	bultin functions
	__builtin_clz  __builtin_ctz     //count the leading and tail zero
	__builtin_popcout                //count the one
	
windows cmd
	fc file1name file2name /n
	
	

	
	
	
	
	
	
	
	
	
 
